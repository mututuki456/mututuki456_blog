---
import BaseLayout from '../layouts/BaseLayout.astro';
import { SITE_TITLE } from '../consts';
---

<BaseLayout title={`404 Not Found | ${SITE_TITLE}`} description="ページが見つかりません" noindex={true}>
  <div class="flex flex-col items-center justify-center text-center px-4">
    {/* メッセージ */}
    <div class="mt-2">
      <p class="text-2xl font-bold text-gray-800 mb-2">ページが見つかりませんでした</p>
      <p class="text-gray-500 mb-8">
        お探しのページは削除されたか、<br class="md:hidden" />
        URLが間違っている可能性があります。
      </p>
      
      {/* トップへ戻るボタン */}
      <a 
        href="/" 
        class="
          inline-block bg-black text-white px-6 py-3 rounded-full font-bold 
          hover:bg-accent hover:text-white transition-colors duration-300
          shadow-lg hover:shadow-xl hover:-translate-y-1
        "
      >
        トップページに戻る
      </a>
    </div>

  </div>
</BaseLayout>